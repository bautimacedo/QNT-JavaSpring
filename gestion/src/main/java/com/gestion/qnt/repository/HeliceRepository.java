package com.gestion.qnt.repository;

import com.gestion.qnt.model.Helice;
import org.springframework.data.jpa.repository.JpaRepository;

public interface HeliceRepository extends JpaRepository<Helice, Long> {
}
